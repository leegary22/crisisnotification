import React from 'react';
import ReactDOM from 'react-dom/client';
import NotificationDashboard from './components/NotificationDashboard.jsx';

ReactDOM.createRoot(document.getElementById('notification-dashboard-root')).render(
  <React.StrictMode>
    <NotificationDashboard />
  </React.StrictMode>
);