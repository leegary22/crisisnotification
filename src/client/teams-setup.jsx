import React from 'react';
import ReactDOM from 'react-dom/client';
import TeamsSetupApp from './components/TeamsSetupApp.jsx';

ReactDOM.createRoot(document.getElementById('teams-setup-root')).render(
  <React.StrictMode>
    <TeamsSetupApp />
  </React.StrictMode>
);