import React from 'react';
import ReactDOM from 'react-dom/client';
import ManagerDashboard from './components/ManagerDashboard.jsx';

ReactDOM.createRoot(document.getElementById('manager-dashboard-root')).render(
  <React.StrictMode>
    <ManagerDashboard />
  </React.StrictMode>
);